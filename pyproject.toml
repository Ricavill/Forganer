[tool.ruff]
line-length = 110
target-version = "py310"
extend-exclude = ["migrations", "mcp_server/.venv", ".venv"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"]  # Depends(...) as a default arg is the standard FastAPI pattern

[tool.pytest.ini_options]
pythonpath = ["."]
